for every index : put that indexed element at the correct index of LHS 

static void insertion(int[] arr){
    for(int i = 0;i<arr.length-1;i++){
        for(int j = i+1;j>0;j--){
            if(arr[j] < arr[j-1]){
                swap(arr,j,j-1);
            }
            else{
                break;
            }
        }
    }
}

static void swap(int[] arr,int first, int second){
    int temp = arr[first];
    arr[first] arr[second];
    arr[second] = temp;
}


stable 
used for smaller values of N => works good with array if partially sorted
it takes  part in hybrid sorting algorithm